<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".user.menu.SetNominalActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".user.menu.ScanActivity" />
        </activity>
        <activity
            android:name=".user.menu.ScanActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".user.menu.MPayIdActivity"
            android:label="@string/mp_id"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".user.ppob.MoreActivity"
            android:label="@string/more"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".user.member.HomeMemberActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".user.merchant.HomeMerchantActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".VerifyLoginActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>